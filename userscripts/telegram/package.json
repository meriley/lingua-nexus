{
  "name": "telegram-nllb-translator-userscript",
  "version": "0.1.0",
  "description": "Integration with self-hosted NLLB translation for Telegram Web",
  "main": "telegram-nllb-translator.user.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --testPathPattern=integration",
    "test:unit": "jest --testPathPattern=unit",
    "test:adaptive": "jest --testPathPattern=adaptive",
    "test:e2e": "playwright test",
    "test:e2e:adaptive": "playwright test adaptive-e2e.spec.js",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.16.11",
    "@playwright/test": "^1.32.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "puppeteer": "^19.7.0",
    "playwright": "^1.30.0"
  },
  "// Jest config is in jest.config.js": true
}